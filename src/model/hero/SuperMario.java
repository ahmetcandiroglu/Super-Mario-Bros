package model.hero;

import java.awt.image.BufferedImage;

public class SuperMario extends Mario{

    public SuperMario(double x, double y, BufferedImage[] leftFrames, BufferedImage[] rightFrames) {
        super(x, y, leftFrames, rightFrames);
    }
}
